//
//  Secrets.swift.example
//  KOLPOP_iOS
//
//  이 파일을 복사해 Secrets.swift로 저장한 뒤 실제 값을 채워 넣으세요.
//  Secrets.swift는 .gitignore에 등록되어 있어 git에 커밋되지 않습니다.
//

import Foundation

enum Secrets {
    static let signUpAPIBaseURL = URL(string: "http://YOUR_SERVER_HOST:PORT/")!
    static let chatSocketURL = URL(string: "ws://YOUR_SERVER_HOST:PORT/ws/chat")!
    static let aiAPIBaseURL = URL(string: "http://YOUR_AI_SERVER_HOST:PORT/")!
    static let festivalAPIBaseURL = URL(string: "https://api.data.go.kr/openapi/tn_pubr_public_cltur_fstvl_api")!
    static let festivalAPIServiceKey = "YOUR_SERVICE_KEY_HERE"
}
